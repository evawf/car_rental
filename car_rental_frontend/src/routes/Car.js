export default function Car(idx) {
  return (
    <div>
      <h2>Car details: {idx}</h2>
      {/* <h4>Name: {car.name}</h4>
        <p>Model: {car.model}</p>
        <p>Type: {car.type}</p>
        <p>Price: ${car.price} / Day</p> */}
    </div>
  );
}
